spring.application.name=digital-twin-backend
server.port=8080

# Database Configuration (MySQL)
# Update these credentials to match your local setup
spring.datasource.url=jdbc:mysql://localhost:3306/digital_twin_db?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=deva 2009
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# JWT Secret (In production, use environment variable)
jwt.secret=secretkeymustbelongenoughforhs256algorithmgeneration1234567890

# WebSocket
spring.main.allow-bean-definition-overriding=true
